
jar {
    baseName = 'kontentsu-processing'
}

sourceSets {
    integrationTest {
        resources.srcDir file('src/test/resources')
    }
    main {
        output.resourcesDir = "$buildDir/classes/main"
    }
}

dependencies {
    compile project(':core')
    testCompile project(':test')
    integrationTestCompile project(':core')
    integrationTestCompile project(':test')
}
